<h1> <%= t('.users') %> </h1>

<% @users.each do |user| %>
  <p> <%= user.name  %> | <%= user.role  %> | <%= user.classroom.name if user.classroom  %></p>

  <%= link_to t('.edit'), edit_admin_user_path(user) %>
<% end %>
