<style>
  .container {
    font-family: Arial, Helvetica, sans-serif;
    max-width: 600px;
    margin: 0 auto;
    padding: 20px;
    border-radius: 20px;
    background-color: #47449b;
    color: #f4f3ff
  }

  h1, h2, h3, p {
    text-align: center;
  }

  p {
    font-size: 18px;
  }

  img.logo {
    display: block;
    margin: 0 auto;
    width: 120px;
    text-align: left;
  }

  .token {
    font-size: 40px;
    font-weight: bold;
    color: #dfdef8;
  }

  btn-container {
    margin-top: 20px;
    display: flex;
    justify-content: space-evenly;
  }

  a.button {
    /* color: #fff; */
    background-color: #93ff90;
    padding: 10px;
    text-decoration: none;
    border-radius: 5px;
  }
</style>

<main class="container">
  <img src="http://localhost:3000/logo-bank.png" alt="Logo Raro Bank" width="60" class="logo">

  <h2>Você recebeu uma nova transferência</h2>
  <h3>Transferência N°:<strong><%= @mailer_receiver_info.id %></strong></h3>
  <p>Olá <%= @mailer_receiver_info.receiver.user.name %>, você recebeu uma nova transferência de <%= @mailer_receiver_info.sender.user.name %> %> num valor de:</p>
  <p class="token"><%= number_to_currency(@mailer_receiver_info.amount) %></p>

</main>
